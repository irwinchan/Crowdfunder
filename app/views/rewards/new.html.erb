<h1>Rewards</h1>

<%= form_for @reward do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.label "pledge" %>
  <%= f.number_field :backer_limit %>

  <%= f.submit %>

<% end %>