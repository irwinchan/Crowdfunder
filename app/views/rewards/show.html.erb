<%= @reward.name %>
<%= @reward.description %>
<%= @reward.backer_limit %>

<%= form_for([@project, @reward, @pledge]) do |f| %>
 	<div class="field">
 		<%= f.label :amount %>
 		<%= f.number_field :amount %>
 	</div>

 	<div class="action">
 		<%= f.submit %>
 	</div>
<% end %>
